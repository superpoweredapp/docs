---
title: "Create OutboundCall — agent"
api: "POST /outbound_call"
description: "This endpoint allows you to start a call. You need to provide a number and agent configuration."
---

<Tip>
  Using the same agent repeatedly? [Save it](/api-reference/agent/create). Then,
  [create call with agentId](/api-reference/call/create).
</Tip>

## Request

### Body

<Snippet file="outbound_call.body.mdx" />

<ParamField body="agent" type="object" required>
  <Expandable title="Agent">
    <Snippet file="agent.body.mdx" />
  </Expandable>
</ParamField>

### Header

<ParamField
  header="Content-Type"
  required
  placeholder="application/json"
></ParamField>

<Snippet file="authorization.header.mdx" />

## Response

<ResponseField name="status" type="string">
  This indicates whether the call was successful.
</ResponseField>
