<ParamField body="name" type="string">
  This is the agent's name, just for your reference.
</ParamField>

<ParamField body="context" type="string">
  This is the AI's objective and the context around the objective.
</ParamField>

<ParamField body="voice" type="string" default="phillip">
  This is the voice that will be used. Options are `ryan`, `steve`, `burt`,
  `phillip`, `joseph`, `sarah`, `joanne`, `mark`, and `maya`.
</ParamField>

<ParamField body="getContextFromCallback" type="boolean" default="false">
  Whether to get the retrieve the agent's context from the callback URL prior to
  starting the call. If true, the context parameter will be ignored. You can use
  this to customize the agent's context based on the caller's phone number, or
  to retrieve the context from a database.
</ParamField>

<ParamField body="callbackUrl" type="string" default="null">
  This is the URL Vapi will send GET / POST requests for retrieving context, function calling, and end-of-call reports. All requests will be sent with `userPhoneNumber`, `botPhoneNumber`, and all the agent parameters.

  <Expandable title="POST: Function Calling">
    For function calling, respond with a JSON object of the result. For example: `{ result: "Hello world!" }` or `{ result: { temperature: "38 degrees" } }`.
    
    If empty, return `{}`.

    <ResponseField name="name" type="string">
      This is the name of the function the agent wants to invoke.
    </ResponseField>

    <ResponseField name="arguments" type="object">
      These are the arguments to the function, as a JSON object.
    </ResponseField>

  </Expandable>

<Expandable title="POST: End-of-Call Report">
  <Snippet file="end.body.mdx" />
</Expandable>

  <Expandable title="GET: Agent Context">
    For providing the agent's context, respond with a object including the context: `{ context: "You are an assistant..." }`.

    <ResponseField name="context" type="string">
      This is the AI's objective and the context around the objective.
    </ResponseField>

  </Expandable>
</ParamField>

<ParamField body="functions" type="array" default="[]">
  <Expandable title="Function">
    <Snippet file="function.body.mdx" />
  </Expandable>
</ParamField>
